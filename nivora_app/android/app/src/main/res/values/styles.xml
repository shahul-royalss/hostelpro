<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!--
          The design's ground, not ?android:colorBackground.

          The inherited attribute is WHITE under this Light parent, and this theme is swapped
          in the moment the process starts — before the first Flutter frame, not after it. So
          the launch window handed off to a white window, which handed off to Flutter painting
          #0B0D0F: two flashes on a light-mode phone, on every cold start. Pinning it to the
          same colour the splash paints makes the whole handoff invisible.

          The values-night/ copy of this file says the same thing for the same reason; the
          colour is deliberately identical in both, because the app's launch is brand-dark
          whatever the phone's theme is. See lib/features/splash/splash_screen.dart.
        -->
        <item name="android:windowBackground">@color/nivora_ground_light</item>
    </style>
</resources>
